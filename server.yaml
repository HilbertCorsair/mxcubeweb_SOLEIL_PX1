---

server:
  SECRET_KEY: ASECRETKEY
  SECURITY_PASSWORD_SALT: ASALT
  PERMANENT_SESSION_LIFETIME: 60
  CERT: NONE

  DEBUG: false

  ALLOWED_CORS_ORIGINS:
    - http://localhost:8081
    - http://127.0.0.1:8081
    - http://localhost:5173
    - http://127.0.0.1:5173
    - ws://localhost:8000
    - ws://127.0.0.1:8000

sso:
  USE_SSO: true
  ISSUER: https://iam-dev.synchrotron-soleil.fr/realms/soleil/
  LOGOUT_URI: ""
  TOKEN_INFO_URI: https://iam-dev.synchrotron-soleil.fr/realms/soleil/protocol/openid-connect/auth
  CLIENT_SECRET: VuJbXd79T8fptZV25usIpPdvdqBlqNke
  CLIENT_ID: mxcube_web_px1_test
  SCOPE: openid email profile
  CODE_CHALLANGE_METHOD: S256

mxcube:
  USE_EXTERNAL_STREAMER: true
  VIDEO_FORMAT: MPEG1
  VIDEO_STREAM_URL: ws://localhost:8000/ws

  # At which port to stream from
  VIDEO_STREAM_PORT: 8000
  # Mode, SSX-CHIP, SSX-INJECTOR, OSC
  mode: OSC

  usermanager:
    class: UserManager
    inhouse_is_staff: true
    users:
      - username: opid291
        role: staff
